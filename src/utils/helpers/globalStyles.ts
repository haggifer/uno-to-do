import {Theme} from "@mui/material/styles";
import {SystemStyleObject} from "@mui/system/styleFunctionSx/styleFunctionSx";

export const getGlobalStyles = (theme: Theme): SystemStyleObject => {
  return {
    // ...getSomeStyles(theme),
    // ...getAnotherStyles(theme),
  }
}